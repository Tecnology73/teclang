func sum(int a, int b) int {
    return a + b;
}

func main() int {
    int result(sum(1, 3));
    int total(result);

    for (int i in 1..result) {
        total *= result;
    }

    printf("%d * %d = %d\n", result, result, total);

    return 0;
}
